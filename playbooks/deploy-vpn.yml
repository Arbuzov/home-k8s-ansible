---
# Deploy / converge the 3x-ui VPN gateway (separate from the k8s cluster).
# Run: ansible-playbook -i inventory-vpn.yml playbooks/deploy-vpn.yml
- name: Deploy 3x-ui VPN gateway
  hosts: vpn
  become: true
  gather_facts: true
  roles:
    - role: vpn
      tags: [vpn]
